---
// Footer Component
---

<footer class="footer-section">
  <div class="container">
    <div class="footer-content">
      <p class="copyright">© 2026 Gerardo Romani</p>
      <a href="https://www.instagram.com/tuousername/" target="_blank" rel="noopener noreferrer" class="instagram-icon" aria-label="Instagram">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
          <rect x="2" y="2" width="20" height="20" rx="5" ry="5"/>
          <path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"/>
          <line x1="17.5" y1="6.5" x2="17.51" y2="6.5"/>
        </svg>
      </a>
      <p class="made-with">Made with <span class="heart">♥</span> and creative energy</p>
    </div>
  </div>
</footer>

<style>
  .footer-section {
    background: var(--dark-bg);
    padding: 3rem 0;
    border-top: 1px solid rgba(44, 95, 158, 0.2);
  }

  .footer-content {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 2rem;
    flex-wrap: wrap;
  }

  .copyright {
    font-family: 'Space Mono', monospace;
    font-size: 0.9rem;
    color: var(--text-gray);
    letter-spacing: 1px;
  }

  .instagram-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--primary-blue), var(--secondary-blue));
    color: white;
    transition: all 0.3s ease;
    box-shadow: 0 5px 15px rgba(44, 95, 158, 0.3);
  }

  .instagram-icon:hover {
    transform: translateY(-5px) rotate(5deg);
    box-shadow: 0 10px 25px rgba(44, 95, 158, 0.5);
  }

  .instagram-icon svg {
    width: 20px;
    height: 20px;
    stroke-width: 2;
  }

  .made-with {
    font-size: 0.9rem;
    color: var(--text-gray);
  }

  .heart {
    color: var(--primary-blue);
    animation: heartbeat 1.5s ease-in-out infinite;
  }

  @keyframes heartbeat {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.2); }
  }

  @media (max-width: 768px) {
    .footer-content {
      flex-direction: column;
      gap: 1rem;
    }
  }
</style>
